# 2026-06-27 工作日志

## 完成的任务

### 1. 统一端口配置 ✅
- **前端 (Vite)**: 端口 **5899** (vite.config.ts)
- **后端 (FastAPI)**: 端口 **8898** (.env.development.local, agent/.env)
- **PEG 服务 (Next.js)**: 端口 **3000** (astock-peg/web)
- 所有 Vite 代理配置已统一指向后端 8898
- 创建 PORT-CONFIG.md 文档记录所有端口配置

### 2. 优化大A看板文件夹 ✅
清理的文件：
- 删除了 12 个 `.DS_Store` 文件
- 删除了前端 `.vite` 缓存目录
- 删除了前端 `dist` 构建目录
- 删除了 `test-proxy.mjs` 测试文件
- 删除了 `temp_download` 临时目录

### 3. 创建启动脚本和 launchd 服务 ✅
启动脚本 (~/.local/bin/):
- `start-vibe-frontend.sh` - 前端启动脚本
- `start-vibe-backend.sh` - 后端启动脚本
- `start-astock-peg.sh` - PEG 启动脚本
- `load-all-services.sh` - 服务加载脚本（手动运行一次）

Launchd 配置 (~/Library/LaunchAgents/):
- `com.local.vibevite.plist` - 前端服务
- `com.local.vibebackend.plist` - 后端服务
- `com.local.astockpeg.plist` - PEG 服务

### 4. 启动所有服务 ✅
- 前端服务 (5899): 已运行
- 后端服务 (8898): 已运行
- PEG 服务 (3000): 已手动启动

## 技术问题排查

### 图表空白 Bug
- **根因**: useECharts hook 的 setOption 在实例初始化前被调用后丢弃
- **修复**: 增加 pendingOptionRef 缓存 + ready state

### 因子库报错
- **根因**: 后端未启动 + alpha 路由未注册 + .env 端口不一致
- **修复**: 启动后端 + 修复路由注册 + 统一端口为 8898

### Vite 代理返回 HTML
- **根因**: `/alpha` 正则匹配失效
- **修复**: 改为字符串匹配 `"/alpha": apiProxy`

### PEG 500 错误
- **根因**: astock-peg Next.js (3000) 未启动
- **修复**: 创建启动脚本并启动服务

### 相关性矩阵超时
- **根因**: 同步阻塞事件循环 + yfinance 限流
- **修复**: 改为线程池执行

## 待完成任务

### Launchd 服务加载
- **问题**: sandbox 限制无法使用 launchctl bootstrap/load
- **错误**: "Bootstrap failed: 5: Input/output error"
- **解决方案**: 用户需要手动运行一次 `~/.local/bin/load-all-services.sh`
- **之后**: 服务会自动开机启动

## 文件夹优化（第二轮）

### 问题
- 用户反馈文件夹有 2.3GB，询问是否可以优化
- 要求：不要影响 Vibe-Trading 的内容和运行，不要出 bug

### 执行的操作

1. **分析目录大小**
   - Vibe-Trading/frontend/node_modules: 276MB
   - astock-peg/web/node_modules: 526MB
   - astock-peg/web/.next: 323MB (构建缓存)
   - Python 缓存: <1MB

2. **创建安全清理脚本** (`CLEANUP-SAFE.sh`)
   - 只删除可以完全恢复的文件
   - 保留所有源代码、配置文件、.env

3. **执行清理**
   - ✅ 删除 Vibe-Trading/frontend/node_modules (276MB)
   - ✅ 删除 astock-peg/web/node_modules (526MB)
   - ✅ 删除 Python __pycache__ 和 .egg-info
   - ✅ 删除所有 .DS_Store 文件
   - ✅ 重新安装依赖（确保功能正常）

4. **修复 PEG 服务问题**
   - ❌ 第一次重新安装后，Next.js 包不完整
   - ✅ 删除 node_modules 和 package-lock.json
   - ✅ 重新运行 npm install (487 packages)
   - ✅ 重启 PEG 服务

### 优化结果

**优化前：**
- 总大小: 2.3GB
- Vibe-Trading: 1.3GB
- astock-peg: 849MB

**优化后：**
- 总大小: 1.6GB (节省 700MB, 30%)
- Vibe-Trading: 1.0GB (节省 300MB)
- astock-peg: 543MB (节省 306MB)

### 验证服务状态

所有 3 个服务已验证正常运行：
- ✅ 前端 (5899): HTTP 200
- ✅ 后端 (8898): HTTP 200
- ✅ PEG (3000): Ready

### 注意事项

1. **为什么依赖重新安装后大小不同？**
   - npm 依赖版本可能更新
   - .next 缓存已删除（会在运行时重新生成）
   
2. **如何进一步减小体积？**
   - 可以删除 node_modules（需要时重新安装）
   - 但可以删除 .next、dist 等构建缓存
   
3. **安全清理脚本**
   - 位置: `/Users/wangzhiping/Claude/大A投研看板/CLEANUP-SAFE.sh`
   - 可重复运行，只删除可恢复文件

## 服务管理命令

```bash
# 查看服务状态
launchctl list | grep com.local

# 手动启动服务（如果 crash）
bash ~/.local/bin/start-vibe-frontend.sh
bash ~/.local/bin/start-vibe-backend.sh
bash ~/.local/bin/start-astock-peg.sh

# 查看日志
tail -f /tmp/vibe-frontend.log
tail -f /tmp/vibe-backend.log
tail -f /tmp/astock-peg.log
```

## 访问地址

- **前端**: http://localhost:5899/agent
- **后端 API**: http://localhost:8898/docs
- **PEG 分析**: http://localhost:3000

## PEG 500 持续修复（最新进展）

### 根因（最终确认）
1. **PEG 服务进程反复崩溃** — 启动后不久就退出
2. **系统 HTTP 代理干扰** — `http_proxy=http://127.0.0.1:61265` 导致 308 死循环
3. **launchd KeepAlive 已生效** — 杀掉进程后会自动重启

### 修复措施
1. `start-astock-peg.sh`: 添加完整 Node.js PATH + 使用 `exec` 替换 shell
2. `com.local.astockpeg.plist`: 改为直接用 node 运行 next dev
3. **验证通过**: `curl --noproxy '*' localhost:3000/api/quotes` → 200 + 正确 JSON

### 前端重构（安全进行中）
- 提取辅助函数 → `src/utils/stockBoard.ts` (changeColor, yoyColorClass, fmtPrice)
- 提取子组件 → `src/components/stockBoard/index.tsx` (SectionHeader, MetricCard)
- StockBoard.tsx: 1389行 → 1345行
- 编译验证: `tsc --noEmit` ✅ + `npm run build` ✅
- 备份在 `frontend/BACKUP-*/`
- **等待用户验证功能正常后继续**

## 项目目录大小

- `astock-peg/`: 847 MB
- `Vibe-Trading/`: 1.3 GB
- 总大小: ~2.1 GB (包含 node_modules 和 Python 依赖)
